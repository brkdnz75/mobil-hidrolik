# App
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace-with-long-random-secret

# Database (SQLite - default)
DATABASE_URL="file:./dev.db"

# Optional Postgres setup
# DATABASE_URL="postgresql://user:password@localhost:5432/hidrolikpro?schema=public"
# For Postgres, copy prisma/schema.postgres.prisma -> prisma/schema.prisma before generate/migrate.

# Admin seed credentials
ADMIN_EMAIL=admin@hidrolikpro.com
ADMIN_PASSWORD=Admin123!

# Mail receiver
INQUIRY_RECEIVER_EMAIL=admin@hidrolikpro.com
MAIL_FROM="HidrolikPro <onboarding@resend.dev>"

# Resend (preferred)
RESEND_API_KEY=

# SMTP fallback
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
